# singbox-manager 路线图

## 当前定位

项目已经不再是早期 `v0.1.x beta` 阶段，而是：

- 可发行
- 可自用/小规模使用
- 以 CLI + TUI + systemd 运维闭环为核心
- 不做 Web 面板

目标保持不变：

- 单机 / 小规模优先
- 围绕 sing-box 配置、用户、流量、订阅、部署诊断展开
- 保持二进制轻量、依赖少、部署简单

## 近期优先级

### P1

1. 发布流程更稳
   - 版本号 / `Cargo.lock` / `CHANGELOG` / tag 一致性检查
   - 发版前自检脚本
   - 减少"本地能发，CI 因锁文件或版本漂移失败"

2. 配置/订阅兼容性继续补齐
   - 各协议生成逻辑回归测试
   - 更多 sing-box 配置边界情况
   - 继续减少"格式对，但客户端不认"的问题

3. 运维侧稳定性
   - backup/restore 再做边界校验
   - 安装脚本/升级脚本继续减少环境假设
   - 降低对发行版差异和路径差异的敏感度

### P2

1. 更完整的 CLI 自检/修复建议
2. 更细的日志与错误分级
3. 更多自动化测试覆盖（当前 65 个，主要是纯函数与 DB 语义；协议生成的回归面还偏薄）
4. 进一步清理历史兼容逻辑（如 `users.manual_bytes` 冗余列）

### v0.5.0 已完成

- 全面审计：安全（绑定码重放、转义、下载校验）、可靠性（优雅退出、gRPC 超时、迁移竞态、锁的取消安全）、一致性（跨重连状态、阈值对齐、多入口导出策略）
- `sb doctor` 增加数据库结构版本检查；内核安装后自动探测 gRPC
- CLI 恢复 `sb bind` 绑定码管理；TUI 模式也启动 TG Bot
- README 补上 Telegram Bot 章节

## 明确不做

- 不做 Web 面板
- 不做复杂 RBAC / 多租户
- 不做平台化后端
- 不做“花哨但收益低”的 TUI 鼠标交互

## 判断标准

后续版本优先看三件事：

1. 部署后是否更稳
2. 出问题时是否更容易定位
3. 发版和升级时是否更少踩坑
